on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "Login to DockerHub"
        run: docker login --username maryam155 --password dckr_pat_Wl3KZGvoLDz1LESuzKflN-6-HRg